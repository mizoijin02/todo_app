name: todoapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.0.0

  # 状態管理
  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.3  # ← 追加！

  # データクラス自動生成
  freezed_annotation: ^2.4.1

  # JSON変換
  json_annotation: ^4.9.0

  # ログ出力
  logger: ^2.0.2+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

  # コード自動生成
  build_runner: ^2.4.7
  riverpod_generator: ^2.3.9  # ← 追加！重要！

  # データクラス生成
  freezed: ^2.4.6

  # JSON変換生成
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true